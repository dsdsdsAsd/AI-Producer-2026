# 🤖 Agentic RAG System

Многопользовательская система для общения с документами на основе LangGraph и OpenAI.

## 📋 Возможности

- ✅ **Интеллектуальная маршрутизация** - агент сам решает, когда использовать RAG, а когда отвечать напрямую
- ✅ **Векторный поиск** - быстрый поиск по документам через Supabase pgvector
- ✅ **Память диалогов** - система помнит контекст разговора
- ✅ **Многопользовательская** - каждый пользователь со своей историей
- ✅ **Streaming ответов** - ответы появляются постепенно (SSE)
- ✅ **Загрузка документов** - поддержка PDF, TXT, DOCX

## 🏗️ Архитектура

### LangGraph граф:
1. **Router Node** - определяет намерение (RAG / Creative / Direct)
2. **RAG Node** - поиск в векторной базе знаний
3. **Generator Node** - генерация финального ответа

### Технологии:
- **Backend**: FastAPI (async)
- **LLM**: OpenAI GPT-4o-mini
- **Векторная БД**: Supabase (pgvector)
- **Память**: PostgreSQL
- **Frontend**: HTML + JavaScript (SSE)

## 🚀 Установка

### 1. Клонируйте репозиторий
```bash
cd "C:\Users\10\Desktop\RAG LANGCHAIN"
```

### 2. Создайте виртуальное окружение
```bash
python -m venv venv
venv\Scripts\activate
```

### 3. Установите зависимости
```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения
Скопируйте `.env.example` в `.env` и заполните своими ключами:
```bash
copy .env.example .env
```

### 5. Инициализируйте базу данных
Выполните SQL скрипт в вашей Supabase PostgreSQL:
```bash
# Откройте Supabase SQL Editor и выполните содержимое database/init_db.sql
```

## 📦 Структура проекта

```
RAG LANGCHAIN/
├── backend/
│   ├── config/          # Настройки и промпты
│   ├── database/        # Работа с БД
│   ├── graph/           # LangGraph узлы и граф
│   ├── tools/           # Инструменты (RAG, классификатор)
│   ├── api/             # FastAPI endpoints
│   └── utils/           # Утилиты (chunking, logger)
├── frontend/            # Веб-чат
├── data/documents/      # Загруженные документы
└── requirements.txt
```

## 🎯 Использование

### Запуск сервера
```bash
cd backend
uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

### Открыть веб-чат
Откройте в браузере: `http://localhost:8000`

### API Endpoints
- `POST /api/auth/login` - Авторизация
- `POST /api/chat/stream` - Отправка сообщения (SSE streaming)
- `POST /api/knowledge/upload` - Загрузка документа
- `GET /api/chat/history` - История чата

## 📚 Примеры использования

### Вопросы по документам (RAG):
```
"О чем говорится в главе 5?"
"Что автор пишет про фотосинтез?"
```

### Творческие задачи:
```
"Перепиши эту главу проще"
"Составь конспект по книге"
```

### Обычное общение:
```
"Привет, как дела?"
"Спасибо за помощь!"
```

## 🔧 Настройка chunking

В `.env` можно настроить параметры разбивки документов:
- `CHUNK_SIZE=1000` - размер чанка в токенах (для книг: 1000-1500)
- `CHUNK_OVERLAP=200` - перекрытие между чанками

## 📝 Лицензия

MIT
