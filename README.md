# 🎬 AI Producer 2026

Интеллектуальная система для создания контент-стратегии, генерации сценариев для видео (Shorts/Reels) и управления контент-планом в виде интерактивного 3D-графа. Система построена на базе мультиагентного RAG, интегрированного с личным опытом пользователя.

## 🌟 Основная идея
Этот проект — ваш личный "AI Продюсер". Он не просто генерирует текст. Он берет вашу:
1. Текущую цель (например, "продать курс за 50 000 руб")
2. Ваше позиционирование (AI Architect)
3. Ваши реальные кейсы (Voice AI, E-comm RAG)
И на основе этого генерирует **виральные темы**, **сценарии с хуками** и выстраивает **контент-план**.

## 🚀 Ключевые фичи

- 🧠 **Стратегический Дашборд**: Единый центр управления, где AI хранит ваш "Эталонный контекст", боли аудитории и форматы монетизации.
- 🕸️ **3D Граф Контента**: Визуализация связей между стратегией, темами и готовыми сценариями с помощью `react-force-graph-3d`. Дает вид "сверху" на всю воронку.
- 🤖 **AI Продюсер (Чат)**: Интеллектуальный помощник на базе LangGraph. Он способен "разгонять" локальные кейсы до уровня глобальных трендов, создавая поляризующий и виральный контент.
- 📋 **Канбан-доска**: Инструмент для перевода идей в готовые к съемке сценарии (Todo -> In Progress -> Done).

## 🛠️ Технический стек

### Frontend (User Interface)
- **React.js + Vite**
- **Tailwind CSS** (стилизация)
- **Framer Motion** (анимации)
- **react-force-graph-3d** (визуализация графа)
- **Lucide React** (иконки рынка)

### Backend (AI & Logic)
- **Фреймворк**: FastAPI (Python)
- **AI Core**: LangChain + LangGraph (Маршрутизация агентов)
- **LLM**: Ollama (Локальные модели) / OpenAI
- **БД**: PostgreSQL (Supabase / Локально)
- **Векторный поиск**: pgvector (для поиска по базе знаний и сценариям)

## 📦 Установка и запуск

### 1. Backend (FastAPI + LangGraph)
В папке `rag_backend`:
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
# Запуск сервера
python -m uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
```

### 2. Frontend (React)
В папке `planner-app`:
```bash
npm install
npm run dev
```

Обе части работают локально и синхронизированы через API.

## 🧩 Структура проекта
```
AI-Producer-2026/
├── planner-app/       # React интерфейс с 3D графом и дашбордами
├── rag_backend/       # FastAPI сервер, LangGraph агенты и БД
├── init_db.py         # Скрипт инициализации схем PostgreSQL
├── seed_strategy.py   # Скрипт посева стратегии и базы
├── CORE_STRATEGY.md   # Ваш манифест позиционирования
└── README.md
```
